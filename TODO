# TODO

* delete old file if source updated (add parameter and pop it in view)
* If char.location is deleted but there are still altchars on it, altchar's source must be reset or altchars must be removed
* create better source browser

* error handling everywhere
* AWS Lambda also handle zip file uploads
* get progress updates for large file uploads (EvaporateJS)
* implement confirmation process for deletes (sources and altchars)
* remove CORS headers
* remove public access from Bucket
